% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zigeom.R
\name{zigeom}
\alias{zigeom}
\alias{dzigeom}
\alias{pzigeom}
\alias{rzigeom}
\title{Zero-inflated geometric distribution}
\usage{
dzigeom(x, prob, zeroprob = 0, log = FALSE)

pzigeom(q, prob, zeroprob = 0, lower.tail = TRUE, log.p = FALSE)

rzigeom(n, prob, zeroprob = 0)
}
\arguments{
\item{x, q}{integer vector of counts}

\item{prob}{probability of success in each trial, in (0,1]}

\item{zeroprob}{zero-inflation probability between 0 and 1}

\item{log, log.p}{logical; return log-density if TRUE}

\item{lower.tail}{logical; if \code{TRUE}, probabilities are \eqn{P[X \le x]}, otherwise, \eqn{P[X > x]}.}

\item{n}{number of random values to return.}
}
\value{
\code{dzigeom} gives the probability mass function, \code{pzigeom} gives the distribution function, and \code{rzigeom} generates random deviates.
}
\description{
Probability mass function, distribution function, and random generation for
the zero-inflated geometric distribution.
}
\details{
This implementation allows for automatic differentiation with \code{RTMB}.

\deqn{P(X=k;\,\pi,p_0) = p_0\,\mathbf{1}[k=0] + (1-p_0)\,P_{\mathrm{Geom}}(k;\,\pi),}
where \eqn{p_0} is \code{zeroprob}. The zeros are a mixture of structural zeros
and zeros generated by the geometric itself, so \code{zeroprob} is not the
probability of observing a zero; see \code{\link{hgeom}} for the hurdle version,
where it is.
}
\examples{
set.seed(123)
x <- rzigeom(5, prob = 0.3, zeroprob = 0.4)
d <- dzigeom(x, prob = 0.3, zeroprob = 0.4)
p <- pzigeom(x, prob = 0.3, zeroprob = 0.4)
}
\seealso{
\link{ztgeom}, \link{hgeom}, \link{zinbinom}, \link{zipois}
}
